#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for ALX School students
 *
 * Return: always 0 (success)
 *
 */

int main(void)
{

char *s = "hello, world";
char *f = "oleh";
unsigned int n;
 
 n = _strspn(s, f);

 if (f != NULL)
   {
     printf("%u\n", n);
   }

return (0);

}
