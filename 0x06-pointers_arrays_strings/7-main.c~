#include "holberton.h"
#include <stdio.h>
/**
 * main - checking code for ALX school students
 *
 * Return: nothing to return
 *
 */
int main(void)
{
char str[] = "Expect the best. Prepare for the worest. Capitalize on what comes.\n hello world! hello world 0123456hello world\thello world.hello world\n";
char *ptr;

ptr = cap_string(str);
printf("%s", ptr);
printf("%s", str);

return(0);
}
