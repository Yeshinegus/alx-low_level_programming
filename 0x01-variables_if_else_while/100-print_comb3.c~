#include <stdio.h>
/**
 * main - C programe enterance
 *
 * Return: always 0 (success)
 *
 */
int main(void)
{
char i, j;
for (i = 48; i < 58; i++)
{

for (j = i; j < 58; j++)
{
  if( i != j && i < j)
    {
putchar(i);
putchar(j);
 if ((i != 56) && (j != 57))
putchar(',');

putchar(' ');
   
   }
    }
}
putchar('\n');
return (0);
}
