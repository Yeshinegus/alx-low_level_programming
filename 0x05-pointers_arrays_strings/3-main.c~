#include "holberton.h"
#include <stdio.h>
/**
 * main - checks code for ALX students
 *
 * Return: always 0 (success)
 */
int main(void)
{
char *str;
int len;
str = "I do not fear computers. I fear the lack of them - Isaac Asimove";
len = _puts(str);

return (0);
}
